import 'package:flutter/material.dart';

abstract final class FnvShadows {
  const FnvShadows._();

  static const card = [BoxShadow(color: Color(0x0D000068), offset: Offset(0, 5), blurRadius: 20)];
}
